<%= link_to "countries index", "/countries" %>
<%= link_to "cities index", "/cities" %>


<h1>All Countries</h1>


<% @countries.each do |country| %>
  <h3><%=  "#{country.name}: cities count =  #{country.cities.length}" %></h3>
<% end %>


<%= link_to "new countries", "/countries/new" %>
